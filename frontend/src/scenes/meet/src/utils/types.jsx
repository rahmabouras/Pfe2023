/*type ToastType = {
  id: string;
  title: string;
  color: "success" | "primary" | "warning" | "danger" | undefined;
};

type BreadCrumbsType = {
  text: string;
  href?: string;
  onClick?: () => void;
};

type MeetingJoinType = "anyone-can-join" | "video-conference" | "1-on-1";

type MeetingType = {
  docId?: string;
  createdBy: string;
  invitedUsers: string[];
  maxUsers: number;
  meetingDate: string;
  meetingId: string;
  meetingName: string;
  meetingType: MeetingJoinType;
  status: boolean;
};

type UserType = {
  email: string;
  name: string;
  uid: string;
  label?: string;
};

type FieldErrorType = {
  show: boolean;
  message: string[];
};
*/