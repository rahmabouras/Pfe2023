import styled from 'styled-components';

import { font } from 'scenes/kanban/shared/utils/styles';

export const Content = styled.div`
  padding: 0 !important;
  ${font.size(15)}
  ${font.regular}
`;
